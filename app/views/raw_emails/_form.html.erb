<%= form_for @raw_email do |f| %>
  <div class="form-group">
    <%= f.label :raw_email, 'Paste in Raw Email' %>
    <%= f.text_area :raw_email, rows: 10, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Save', class: 'btn btn-success btn-block' %>
  </div>
<% end %>
